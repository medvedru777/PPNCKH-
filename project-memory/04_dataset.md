## Dataset — Verified Facts

- Canonical dataset file used for experiments (verified): `data/raw/ember_1k_real.parquet`
	- Shape: 1000 rows x 6 columns (verified programmatically)
	- Label distribution: 500 samples label=0 (benign) and 500 samples label=1 (malware)

- Note about legacy/stale files (verified):
	- `ember_1k_balanced.parquet` (repository root) contains 500 rows and only label=0 — this file is stale/outdated and should not be used as the canonical dataset.

## Planned (not implemented here)

- Detailed schema documentation and full feature listing to be populated after final EDA (not yet recorded here).

